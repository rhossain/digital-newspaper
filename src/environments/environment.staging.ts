// Staging environment — points to the nepaper WordPress installation.
// Build with: ng build --configuration staging
// Serve with:  ng serve --configuration staging
export const environment = {
  production: true,
  wpBaseUrl: 'https://nepaper.dailysangram.com/wp',
  demo: false,
};
